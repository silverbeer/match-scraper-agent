__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
# .env in repo root (ad-hoc overrides) — envs/ dir is tracked
.env
# Prod env contains Supabase credentials
envs/.env.prod
# uv.lock is committed — it pins the exact mls-match-scraper commit used in the Docker image
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
# K8s secrets (use secret.yaml.example as template)
k3s/**/secret.yaml
# Ad-hoc backfill jobs — generated locally, applied directly (see docs/backfill.md)
k3s/backfill/
